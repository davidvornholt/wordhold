// The wordmark in the home shell's top bar: a link home, styled once so the
// router and the fixture app render the identical element.
export const wordmarkClass =
  'inline-flex min-h-11 items-center font-display font-semibold text-lg tracking-tight focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring';
