interface:
  display_name: "Declarative Infrastructure"
  short_description: "Operating contract for NixOS/OpenTofu infrastructure"
  default_prompt: "Use $declarative-infra when touching infrastructure, even if its home is another repo."
