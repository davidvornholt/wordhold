# Plaintext shape only. Do not put real preview credentials in this file.
# The matching SOPS target is decryptable only by the operator and the
# protected `pr-preview` GitHub environment.
ci:
  ssh_private_key: |-
    -----BEGIN OPENSSH PRIVATE KEY-----
    replace-with-the-dedicated-wordhold-preview-deploy-key
    -----END OPENSSH PRIVATE KEY-----
